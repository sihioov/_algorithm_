// DFS.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include <windows.h>
//template <typename T>
//struct Node
//{
//    Node* next;
//    T data;
//};
using namespace std;
template <typename T>
class C_Node
{
public:
    C_Node* next;
    T data;
};

struct data {
    int a;
    int b;
    char c;
    char d;
};

//void search(Node* root);
//void visit(Node node);



void search(void* root)
{
    if (root == 0) return;
}


void overlap()
{
    HANDLE  h = INVALID_HANDLE_VALUE;
}

int main()
{
    /*int a[100];

    for (int i = 0; i <= 100; i++)
    {
        a[i] = i;
    }*/
    while (1)
    {

    
    WORD a = 1;
    DWORD b = 2;
    int c = 3;
    long d = 4;
    float e = 5;
    long long f = 6;
    printf("a : %d, size : %d\n", a, sizeof(a));
    printf("b : %d, size : %d\n", b, sizeof(b));
    printf("c : %d, size : %d\n", c, sizeof(c));
    printf("d : %d, size : %d\n", d, sizeof(d));
    printf("e : %f, size : %d\n", e, sizeof(c));
    printf("d : %l64d, size : %d\n", f, sizeof(f));
    double isThisPossible[1024];

    Sleep(1000);
    }
}

